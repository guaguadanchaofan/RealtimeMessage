version: "3.9"
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  dingbot:
    build: .
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    depends_on:
      - redis
